/**
 * PriorityQueue
 * Implemented as a binary heap.
 */
function PriorityQueue() {
    this.queue = [];
    this.size = 0;

    this.add = function(data, priority) {
    }

    this.poll = function() {
    }

    this.heapUp = function() {
    }

    this.heapDown = function() {
    }

    this.swap = function(indexA, indexB) {
    }
}